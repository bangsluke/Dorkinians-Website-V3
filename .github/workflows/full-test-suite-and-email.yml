name: Full test suite and summary email

on:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - main
  schedule:
    # Tuesday 02:17 UTC — weekly health check
    - cron: '17 2 * * 2'
  workflow_dispatch:

concurrency:
  group: full-test-suite-${{ github.ref }}-${{ github.event_name }}
  cancel-in-progress: true

jobs:
  full_test_suite:
    name: Full test suite and email
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run full test matrix and send summary email
        timeout-minutes: 60
        env:
          WEBSITE_URL: ${{ secrets.WEBSITE_URL != '' && secrets.WEBSITE_URL || 'https://dorkinians-website-v3.netlify.app' }}
          HEADLESS: 'true'
          CI: 'true'
          SMTP_SERVER: ${{ secrets.SMTP_SERVER }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          SMTP_FROM_EMAIL: ${{ secrets.SMTP_FROM_EMAIL }}
          SMTP_TO_EMAIL: ${{ secrets.SMTP_TO_EMAIL }}
          SMTP_EMAIL_SECURE: ${{ secrets.SMTP_EMAIL_SECURE || 'false' }}
          SEND_TEST_EMAILS: 'true'
          WORKFLOW_TRIGGER_LABEL: "${{ github.event_name == 'pull_request' && format('pull_request {0} → {1} ({2})', github.head_ref, github.base_ref, github.sha) || format('{0} @ {1} ({2})', github.event_name, github.ref_name, github.sha) }}"
        run: npm run test:weekly:email

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: __tests__/e2e/playwright-report/
          retention-days: 30

      - name: Upload screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-screenshots
          path: __tests__/e2e/test-results/screenshots/
          retention-days: 30
